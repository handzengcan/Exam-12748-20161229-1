<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC  
      "-//mybatis.org//DTD Config 3.0//EN"  
      "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
	<!-- 设置别名 -->
	<typeAliases>
		<package name="com.test.pojo"/>
	</typeAliases>

	<mappers>
		<!-- 可以省略 和Spring整合后，使用mapper扫描器  这里不需要配置了 -->
	</mappers>
</configuration> 